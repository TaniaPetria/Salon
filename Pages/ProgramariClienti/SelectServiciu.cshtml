@page
@model Salon.Pages.ProgramariClienti.SelectServiciuModel

@{
   
}



<h3><a class="btn btn-primary"  asp-page="./Index"><</a> Specialist ales</h3>

<div class="row">
    <div class="col-md-8" style="max-width:500px">
        <hr />
       
        <dl class="row">
            @*<dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Angajat.Nume.ToUpper())
            </dt>*@
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Angajat.Nume)
            </dd>

        
        </dl>
        <hr />
        <h5>Alege specialist</h5>
          <br />
                @foreach (var srv in Model.AssignedServiciuDataList)
                {
                        if (srv.Assigned)
                        {
                            var parms = new Dictionary<string, string>
                                {
                                    { "serv",  srv.ServiciuID.ToString() },
                                    { "spec", @ViewBag.spec.ToString() }
                                };
                            <div class="d-flex flex-row w-100">
                                <div class="w-75">
                                    <h6>  @srv.Name </h6>
                                    <span>Pret:</span> @srv.Pret  <span>lei, Durata:</span> @srv.Durata <br />
                                    <p style="font-size:.8rem;"> @srv.Descriere </p>
                                </div>
                                <div class="d-flex flex-row align-items-center justify-content-end">
                                @*<button class="btn btn-primary" onclick="SelectName('select_client.html',400,500)" type="button">Programeaza</button>*@
                                <a class="btn btn-primary" asp-page="./SelectProgramare" asp-all-route-data="parms">Programeaza</a>

                    </div>
                            </div>
                            <hr />
                        }
                }
                 
                
        
       
    </div>
</div>

<div>
    
    @*<button>Click</button>*@
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
